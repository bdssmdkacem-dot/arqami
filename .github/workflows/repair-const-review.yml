name: Repair const ReviewActivityConfig

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time repair already completed; workflow retained but disabled."